includes:
    - ./vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon
    - phpstan-phpstorm.neon

parameters:
    level: 6

    paths:
        - app
        - config
        - database
        - routes

    excludePaths:
        - tests/*
